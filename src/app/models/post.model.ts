// Definición de la interfaz Post
export interface Post {
  id: number;      // Identificador numérico único para cada publicación
  title: string;   // Título de la publicación, tipo string
  body: string;    // Cuerpo o contenido de la publicación, tipo string
}
